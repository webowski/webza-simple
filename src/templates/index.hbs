<!DOCTYPE html>
<html lang="{{lang}}">
<head>
	{{> partials/meta}}
	{{> partials/head-includes}}
</head>
<body>
	<div class="Wrap">
		{{> partials/header}}

		<main id="main-content">

			<div class="Section">
				<div class="container">

					<h1>{{title}}</h1>
					<ul>
						{{!-- {{#each nav}}
							<li><a href="{{url}}">{{title}}</a></li>
						{{/each}} --}}
					</ul>

				</div>
			</div>

		</main>

		{{> partials/footer}}
	</div>

	{{> partials/footer-includes}}
</body>
</html>
