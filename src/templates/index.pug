extends base/layout.pug

block variables
	- var title = 'Главная'
	- var currentUrl = '/'

block main
	include ./partials/hero

	<div class="Section">
		<div class="container">

			<h1>#{title}</h1>

		</div>
	</div>
